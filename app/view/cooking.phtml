<?php ob_start();?>
    <div class="text-center">
        <h1>Chercher une recette</h1>
        <hr>
    </div>
    <div class="d-flex justify-content-center">
        <form class="row w-100" method="post" action="index.php?page=cooking" id="plats"><!-- partie recherche seule -->
            <select class="custom-select col-lg m-3" name="type" size=1> <!--Liste déroulante des types de plats (entrées,desserts...)-->
                <?php foreach($typeList as $type) { ?>
                    <option><?= htmlspecialchars($type->getLabel()) ?></option>
                <?php } ?>
            </select>
            <input required class="col-lg form-control m-3" type="text" name="search" placeholder="Nom de recette">
            <input class="col-lg btn btn-primary m-3 buttonNav" type="submit" value="Rechercher">
            <a class="col-lg btn btn-primary m-3 buttonNav">Recette aléatoire</a>
        </form>
    </div>
    <hr>
    <article class="jumbotron p-2">
        <div classe="col-md-12 text-center">
            <div class="d-flex justify-content-center my-3"><h1 class="font-weight-bold"><?= htmlspecialchars(\ForceUTF8\Encoding::toUTF8($recipe->getName())) ?></h1></div>
            <div class="d-flex justify-content-center"><img class="col-lg-3" src="<?= htmlspecialchars($recipe->getPicture()) ?>" alt="Image recette"></div>
            
            <div class="row my-4 text-center">
                <div class="col-md-12">
                    <div class="row">
                        <div class="col-md-12 font-weight-bold h4">
                            Temps total : 
                            <?php
                                $tmp = explode(':',$recipe->getTotalTime());
                                $h = intval($tmp[0]);
                                $m = intval($tmp[1]);
                            
                                echo($h > 0 ? htmlspecialchars($h)." heure".($h > 1 ? 's' : '') : '');
                                echo($h > 0 && $m > 0 ? " et " : '');
                                echo($m > 0 ? htmlspecialchars($m)." minute".($m > 1 ? 's' : '') : '');
                            ?>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6 h5">
                            Temps de préparation :
                            <?php
                                $tmp = explode(':',$recipe->getPreparationTime());
                                $h = intval($tmp[0]);
                                $m = intval($tmp[1]);
                            
                                echo($h > 0 ? htmlspecialchars($h)." heure".($h > 1 ? 's' : '') : '');
                                echo($h > 0 && $m > 0 ? " et " : '');
                                echo($m > 0 ? htmlspecialchars($m)." minute".($m > 1 ? 's' : '') : '');
                            ?>
                        </div>
                        <div class="col-md-6 h5">
                            Temps de cuisson :
                            <?php
                                $tmp = explode(':',$recipe->getCookingTime());
                                $h = intval($tmp[0]);
                                $m = intval($tmp[1]);
                            
                                echo($h > 0 ? htmlspecialchars($h)." heure".($h > 1 ? 's' : '') : '');
                                echo($h > 0 && $m > 0 ? " et " : '');
                                echo($m > 0 ? htmlspecialchars($m)." minute".($m > 1 ? 's' : '') : ''); 
                            ?>
                        </div>
                    </div>
                </div>
            </div>
            <hr>
            <div class="row text-center">
                <div class="col-md-3 mt-3">
                    <p class="font-weight-bold">Note : <?= htmlspecialchars($recipe->getScore()) ?>/5</p> 
                </div>
                <div class="col-md-3 mt-3">
                    <p class="font-weight-bold">Difficulté : <?= \ForceUTF8\Encoding::toUTF8($recipe->getDifficulty()) ?></p>
                </div>
                <div class="col-md-3 mt-3">
                    <p class="font-weight-bold">Budget : <?= \ForceUTF8\Encoding::toUTF8($recipe->getPrice()) ?></p>
                </div>
                <div class="col-md-3 mt-3">
                    <p class="font-weight-bold">Personne : <?= \ForceUTF8\Encoding::toUTF8($recipe->getNbPerson()) ?></p>
                </div>                
            </div>
            <hr>
            <div class="row my-3">
                <div class="col-md mb-5">
                    <h2 class="text-center">Etapes de préparation :</h2>    
                    <ul class="p-4">
                        <?php 
                            $i = 0;
                            $lines = explode('<br>',$recipe->getSteps());
                            $limit = count($lines);
                            foreach ($lines as $line) { 
                                if (++$i < $limit) { ?>
                                    <li><?= htmlspecialchars($i.'. '.\ForceUTF8\Encoding::toUTF8($line)) ?></li>
                                <?php }    
                        } ?>
                    </ul>
                    <?php if (!isset($trustedPost['recipeID'])) { ?>
                        <form method="post" action="index.php?page=cooking" class="text-center">
                            <input type="hidden" name="recipeID" value="<?= htmlspecialchars($recipe->getId()) ?>">
                            <input type="submit" class="btn btn-success" value="Je prépare cette recette"/>
                        </form>
                    <?php } else echo("J'ai déjà préparé cette recette"); ?>
                </div>
                <div class="col-md text-center mb-5">
                    <h2>Liste des ingrédients :</h2> 
                    <ul class="p-4">
                        <?php 
                            $i = 0;
                            $lines = explode('<br>',$recipe->getIngredients());
                            $limit = count($lines);
                            foreach ($lines as $line) { 
                                if (++$i < $limit) { ?>
                                    <li><?= htmlspecialchars('* '.\ForceUTF8\Encoding::toUTF8($line)) ?></li>
                                <?php }    
                        } ?>
                    </ul>
                    <?php 
                    $test = str_replace("'", "&#146", $recipe->getIngredients());
                    ?>
                    <button class="btn btn-success" onclick="downloadFile('<?= $test ?>')">Je télécharge la liste d'ingrédient</button>
                </div>
            </div>
        </div>
    </article>
    <script>
        function downloadFile(data, name = 'listeIngredients', type = 'text/plain') {
            const { createElement } = document
            const { URL: { createObjectURL, revokeObjectURL }, setTimeout } = window
            
            while(data.search("<br>") != -1) {
                data = data.replace("<br>", "\n")
            }
            
            const blob = new Blob([data], { type })
            const url = createObjectURL(blob)

            const anchor = document.createElement("a")
            anchor.setAttribute('href', url)
            anchor.setAttribute('download', name)
            anchor.click()
            
            setTimeout(() => { revokeObjectURL(url) }, 100)
            }
    </script>
<?php $sectionContent=ob_get_clean();