<?php ob_start();?>
    <article class="theme">
        <h1>Actualité</h1> 
        <hr>
        <div class="row article"> 
            <ul>
                <?php foreach ($articleList as $article) { ?>
                    <li>
                        <div class="card col-lg-12">
                            <div class="card-header">
                                <a href="<?= htmlspecialchars($article->getURL()) ?>">Article</a>
                                <?= htmlspecialchars(explode(':',$article->getReadingTime())[1]) ?> min <?= htmlspecialchars(explode(':',$article->getReadingTime())[2]) ?> sec
                            </div>
                            <br>
                            <p>
                                <?= htmlspecialchars(\ForceUTF8\Encoding::toUTF8($article->getSummary())) ?>
                            </p>
                        </div>
                    </li>
                <?php } ?>
            </ul>   
        </div>
    </article>
<?php $sectionContent=ob_get_clean();