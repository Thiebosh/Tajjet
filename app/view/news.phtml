<?php ob_start();?>
    <article class="theme">
        <h1>Actualité</h1> 
        <hr>
        <div class="container"> 
            <ul>
                <?php foreach ($articleList as $article) { ?>
                    <li class="my-3">
                        <div class="card col-lg-12">
                            <div class="card-header">
                                <a href="<?= $article->getURL() ?>">Article Title /!\ NEED /!\ </a>
                                <p class="mb-0">Temps aproximatif de l'article : <?= explode(':',$article->getReadingTime())[1] ?> min <?= explode(':',$article->getReadingTime())[2] ?> sec</p>
                            </div>
                            <br>
                            <p>
                                <?= \ForceUTF8\Encoding::toUTF8($article->getSummary()) ?>
                            </p>
                        </div>
                    </li>
                <?php } ?>
            </ul>   
        </div>
    </article>
<?php $sectionContent=ob_get_clean();