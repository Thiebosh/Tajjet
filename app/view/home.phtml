<?php ob_start(); ?>
    <h1 class="col-lg mt-4 mb-4">Page d'Accueil</h1>
    <hr>
    <?php 
        $first = true;
        foreach($grid as $line) { ?>
        <div class="row <?= $first? 'mt-4' : '' ?>">
            <?php foreach($line as $card) { ?>
                <a class="col-lg mt-2" href="<?= $card["active"] ? 'index.php?page='.$card["link"] : '' ?>">
                    <button class="col-lg-12 btn btn-primary <?= !$card["active"] ? 'inactive' : '' ?>">
                        <?= $card["display"] ?><br>
                        <?= !$card["active"] ? "Veuillez renseigner votre ".$card["missing"]." dans votre profil" : '&nbsp;' ?>
                        <!-- si pas truc générique (style image), utiliser switch
                        < ?php switch ($card["link"]) {
                            case "sport": ?>
                                html
                            < ?php break;

                            case "health": ?>
                                html
                            < ?php break;

                            ...
                        } ?>
                        -->
                    </button>
                </a>
            <?php } ?>
        </div>
    <?php 
        $first = false;
        } ?>
<?php $sectionContent = ob_get_clean();
